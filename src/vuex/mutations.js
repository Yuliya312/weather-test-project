export default {
  SET_CITY_WEATHER: (state, cityWeather) => {
    state.cityWeather = cityWeather;
  },
  SET_CURRENT_CITY: (state, currentCity) => {
    state.currentCity = currentCity;
  }
} 